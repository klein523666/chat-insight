# Chat Insight v0.1.2

本补丁版本将报告提示词从一次性的自适应指令升级为任务级连续迭代状态。

## 新增

- 任务列表显示当前迭代提示词摘要；编辑任务时可查看并直接修改完整提示词。
- 自动迭代模式先以当前提示词生成本次报告，成功后再结合本次不可信原始消息产生并保存
  下一轮完整提示词。
- 手动固定模式保留用户输入，不会在报告后自动改写。

提示词迭代失败不会影响当前报告，也不会清除上一轮提示词。聊天内容始终是不可信数据，
无法改变系统边界或调用工具。
